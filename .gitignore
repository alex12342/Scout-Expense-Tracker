# VCS / editor
.git
.idea
.vscode
.opencode

# Dependencies
node_modules
**/node_modules

# Build outputs
**/dist
*.tsbuildinfo
artifacts/*/dist

# Local env / secrets
.env
.env.*
!.env.example

# Local runtime data (postgres data, jwt secret, etc.)
data
**/data
app-data

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / caches
.DS_Store
.cache
.turbo
.playwright-mcp
.codebase-memory

# Local release helper (prompts for credentials at runtime — never commit)
scripts/release.sh
